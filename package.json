{
  "name": "wb-design",
  "version": "1.0.1",
  "private": true,
  "description": "Codex plugin for brainstorming and prototyping WeBank mobile product designs.",
  "scripts": {
    "changeset": "changeset",
    "version-packages": "changeset version && npm install --package-lock-only --ignore-scripts --no-audit && npm run sync:version",
    "sync:version": "node scripts/sync-plugin-version.mjs",
    "check:version": "node scripts/sync-plugin-version.mjs --check",
    "preview": "npm --prefix skills/brainstorm run preview --",
    "session:report": "npm --prefix skills/brainstorm run session:report --",
    "test": "npm --prefix skills/brainstorm test"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.1"
  }
}
